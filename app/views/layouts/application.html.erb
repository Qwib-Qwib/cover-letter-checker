<!DOCTYPE html>
<html>
  <head>
    <title>CoverLetterChecker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class='navbar'>
      <div class='home-link-wrapper'>
        <%= link_to 'Est-il compatible ?', root_path %>
      </div>
      <% if user_signed_in? %>
        <div class='profile-links-wrapper signed-in'>
          <div class='username'><%= current_user.username %></div>
          <div class='user-links'>
            <%= link_to 'Mes offres', new_user_session_path %>
            <%= link_to 'Modifier mes informations', edit_user_registration_path %>
            <%= link_to 'Me déconnecter', destroy_user_session_path, data: {turbo_method: :delete} %>
          </div>
        </div>
      <% else %>
        <div class='profile-links-wrapper not-signed-in'>
          <%= link_to "Inscription", new_user_registration_path %>
          <%= link_to "Connexion", new_user_session_path %>
        </div>
      <% end %>
    </div>

    <div class='container'>
      <%= yield %>

      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
    </div>
  </body>
</html>
